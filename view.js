const numberButtons = document.querySelectorAll(".numberButtons");
const operationButtons = document.querySelectorAll(".operationButtons");
const cleanerButton = document.getElementById("cleanerButton");
const backSpaceButton = document.getElementById("backSpase");
const inputWindow = document.getElementById("inputWindow");
export { numberButtons, operationButtons, cleanerButton, backSpaceButton, inputWindow, };